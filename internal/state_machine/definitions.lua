local definitions = {
  reset_state = {
    key_sequence = "",
    last_context = "main",
    internal_mode = "reset",
    edit_mode = "normal",
    time = os.time(),
    macro_recording = false,
    macro_register = "",
  }
}

return definitions
