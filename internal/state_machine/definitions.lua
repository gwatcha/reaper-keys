local definitions = {
  reset_state = {
    key_sequence = "",
    last_context = "main",
    mode = "normal",
    time = os.time(),
    macro_recording = false,
    macro_register = "",
  }
}

return definitions
