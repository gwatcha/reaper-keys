local definitions = {
  reset_state = {
    key_sequence = "",
    context = "main",
    mode = "normal",
    time = os.time(),
    macro_recording = false,
    macro_register = "",
  }
}

return definitions
