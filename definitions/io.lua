-- starting to learn how to handle devices in code.

return {
  --  rename to midi_in ??
  midi = {
    vkb = 'Virtual Midi Keyboard', -- virtual keyboard | default
    qmk = 'Ergodox EZ',
    roland = 'Roland %- RD Series %- Port 1' -- my roland grand 88 key
  },
  audio = {
    -- ??
  }
}
